eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7500/eureka/
    
spring:
  application:
    name: config-server
  
  cloud:
    config:
      server:
        git:
          uri: 
          
server:
    port: 7510
  